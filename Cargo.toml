[package]
name = "rustc_watt_runtime"
version = "0.0.0"
edition = "2021"
description = "WebAssembly runtime for executing procedural macros (from watt)"
license = "MIT OR Apache-2.0"

[dependencies]
rustc_proc_macro = { path = "../rustc_proc_macro" }

[lib]
path = "src/lib.rs"
